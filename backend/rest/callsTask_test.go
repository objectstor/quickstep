package qrouter

import (

	//"log"

	"fmt"
	"testing"
)

func TestTask(t *testing.T) {
	//server, superToken, err := authAndGetToken("super", "password")
	//assert.Nil(t, err)
	//client := &http.Client{}
	//defer server.Close()
	fmt.Printf("BUMMER\n")
}
